export default function WorkspaceSettingsPage() {
  return (
    <section className="rounded-xl border border-zinc-200 bg-white p-6">
      <h2 className="text-xl font-semibold text-zinc-900">Settings and Defaults</h2>
      <p className="mt-2 text-sm text-zinc-600">
        Configure quote and invoice defaults in the next story. Use workspace
        navigation to browse clients, service packages, quotes, and invoices.
      </p>
    </section>
  );
}
